//! `subtext` CLI — thin wrapper over `subtext::check_pdf` (file I/O, pdfium
//! binding, human/`--json` rendering, batch loop). Wired up after the Phase 1
//! skeleton lands; this stub only proves the scaffold compiles and runs.

fn main() {
    eprintln!("subtext: scaffold only — checker not yet wired (Phase 1 in progress)");
    std::process::exit(2);
}
